server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rk_refinery_db
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

app:
  jwt:
    secret: "CHANGE_ME_TO_A_LONG_RANDOM_SECRET_64_CHARS_MINIMUM_CHANGE_ME_1234567890"
    expirationMinutes: 720